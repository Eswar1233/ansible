plugin: amazon.aws.aws_ec2
region:
- us-east-1   # shows all the us east regions servers
filters:
  tag:Name: frontend
  instance-state-name: running
hostnames:
  - ip-address
compose:
  ansible_host: private_ip_address